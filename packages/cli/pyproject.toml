[project]
name = "jukebox-cli"
version = "0.1.0"
description = "Jukebox CLI (placeholder -- scaffold only, no implementation yet)"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
jukebox-cli = "jukebox_cli.cli:main"

[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jukebox_cli"]
