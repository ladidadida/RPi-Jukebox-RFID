# Runtime dependencies for the Raspberry Pi installer and Docker builds (both still pip-based --
# Track B in documentation/developers/roadmap-core-architecture.md hasn't moved them to uv yet).
# You need to install these with `python -m pip install --no-cache-dir -r requirements.txt`
#
# Dev tooling (uv, ruff, pyright, pytest, ...) uses pyproject.toml instead -- see `uv sync --group
# dev`. Keep the two in sync by hand until the installer/Docker are migrated.

# Note:
# sudo apt install libasound2-dev required on some machines

# Jukebox Core
# For USB inputs (reader, buttons) and bluetooth buttons
evdev
mutagen
pulsectl
python-mpd2
ruamel.yaml
# For playlistgenerator
requests
# For the publisher event reactor loop:
tornado
# FastAPI/uvicorn API server (replacing the Tornado bridge, see
# documentation/developers/roadmap-core-architecture.md)
fastapi
uvicorn

# RPi's GPIO packages:
# Shim that preserves the RPi.GPIO API on kernels >= 6.6 (Trixie),
# where /sys/class/gpio/ is gone. See https://github.com/MiczFlor/RPi-Jukebox-RFID/issues/2313
rpi-lgpio --no-binary=lgpio
gpiozero

# Debian and Raspberry Pi OS installations provide PyZMQ through the
# python3-zmq package so it can use the system libzmq.
